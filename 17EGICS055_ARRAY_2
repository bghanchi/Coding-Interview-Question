#include<stdio.h>
void main()
{
	int a[1000000];
	int i,j,n,m,s1=0,s2=0;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	m=n/2;
	for(i=0,j=m;i<m,j<n;i++,j++)
	{
		s1=s1+a[i];
		s2=s2+a[j];
	}
	if(s1>s2)
	printf("%d",s2);
	else
	printf("%d",s1);
}
