#include<stdio.h>
void main()
{
	int i,n,j,a[10],f[10]={0},r=0;
	for(i=0;i<10;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0;i<10;i++)
	{
		while(a[i]!=0)
		{
			n=a[i]%10;
			f[n-1]++;
			a[i]=a[i]/10;
		}
	}
	for(i=0;i<10;i++)
	{
		if(f[i]>=r)
		{
			r=f[i];
			j=i+1;
		}
	}
	printf("\n%d",j);	
}
