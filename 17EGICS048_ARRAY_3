#include<stdio.h>
#include<stdlib.h>
void main()
{
	int i,n,*f={0};
	printf("enter size of an array:");
	scanf("%d",&n);
	int a[n];
	f=(int*)malloc(n*sizeof(int));
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++)
	{
		f[a[i]]++;
	}
	for(i=0;i<*f;i++)
	{
		if(f[i]==1)
		{
			printf("\n%d",i);
		}
	}	
}
